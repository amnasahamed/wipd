# Database
# Local dev (SQLite)
DATABASE_URL="file:./dev.db"
#
# Postgres (recommended for production)
# DATABASE_URL="postgresql://user:password@localhost:5432/dbname?schema=public"

# Vercel Postgres (Generated automatically by Vercel)
# POSTGRES_PRISMA_URL=""
# POSTGRES_URL_NON_POOLING=""

# Security Configuration (REQUIRED for production)
# This key is used to encrypt sensitive configuration values like API keys
# Generate a strong key with: openssl rand -base64 32
CONFIG_ENCRYPTION_KEY="your-secure-encryption-key-here-min-32-chars"

# NextAuth Configuration (Optional but recommended)
# NEXTAUTH_SECRET="your-secret-here"
# NEXTAUTH_URL="http://localhost:3000"

# Optional: Override default ports
# PORT=3000
# HOSTNAME=localhost
